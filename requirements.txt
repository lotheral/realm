# REALM v0.20.0 dependency surface (Sprint 20 hygiene pass, 2026-08-18).
#
# This file pins the production-relevant Python packages REALM imports.
# `pyproject.toml` is the source of truth for declared dependencies + extras;
# requirements.txt is the reproducibility-pinned subset for `pip install -r`.
#
# Generated 2026-08-18 from `pip freeze` filtered to declared imports.
# Update whenever pyproject.toml changes.

# The realm package itself (editable) — without this, importlib.metadata
# cannot resolve the version and realm.__version__ reports 0.0.0.dev0.
-e .

# ---- core ----------------------------------------------------------------
skyfield==1.54
pydantic==2.13.3
PyYAML==6.0.3
python-dotenv==1.2.2
numpy==2.4.4
Faker==40.15.0
networkx==3.6.1
feedparser==6.0.12
httpx==0.28.1

# ---- web API (core product — realm_start.bat serves it) ------------------
fastapi==0.136.0
uvicorn==0.45.0

# ---- preferred ephemeris backend (Skyfield is fallback) -----------------
kerykeion==5.12.8

# ---- LLM (optional; used only when REALM_LLM_*_BACKEND is set) ----------
# Uncomment whichever provider you have credentials for:
openai==2.32.0
# ollama>=0.4

# ---- dev / test ----------------------------------------------------------
pytest==9.0.3
pytest-asyncio==1.3.0
ruff==0.15.11
